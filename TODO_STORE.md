# Next storefront steps

1. Replace placeholder catalog entries with final products, real prices, sizes and SKUs.
2. Deploy the checkout Worker and set `window.STARGIRLS_STORE_API` in `store-config.js`.
3. Add Stripe webhook handling for paid orders.
4. Route Apliiq merch SKUs to Apliiq after payment.
5. Route stocked clothing SKUs to STARGIRLS inventory/self-fulfillment.
6. Add order confirmation and tracking flow.
7. Test a real low-value order before launch.
