/* STARGIRLS product conversion enhancements. No fake scarcity, fake reviews, or fake discounts. */
(()=>{
'use strict';
const SNAPSHOT_KEY='stargirls-clicked-product-v1';
const CACHE_KEYS=['stargirls-printful-catalog-v6','stargirls-printful-catalog-v5'];
const WISH_KEY='stargirls-wishlist-v1';
const RECENT_KEY='stargirls-recent-v1';
const app=document.getElementById('app');
if(!app)return;
const q=new URLSearchParams(location.search);
const requestedId=q.get('id')||'';
const requestedPf=Number(q.get('pf')||(/^printful-(\d+)$/.exec(requestedId)?.[1]||0));
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safe=u=>/^https:\/\//i.test(String(u||''))?String(u):'';
const uniq=a=>[...new Set(a.filter(Boolean))];
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n||0));
const read=(key,fallback=[])=>{try{const v=JSON.parse(localStorage.getItem(key)||'null');return v??fallback}catch{return fallback}};
const write=(key,v)=>{try{localStorage.setItem(key,JSON.stringify(v))}catch{}};
function readCatalog(){for(const key of CACHE_KEYS){try{const raw=JSON.parse(localStorage.getItem(key)||'null');const data=raw?.data||raw;if(data&&Array.isArray(data.products)&&data.products.length)return data;}catch{}}return null;}
function currentSource(){try{const snap=JSON.parse(sessionStorage.getItem(SNAPSHOT_KEY)||'null');if(snap?.source&&(!requestedPf||Number(snap.pfid)===requestedPf))return snap.source;}catch{}const cat=readCatalog();return (cat?.products||[]).find(p=>Number(p.id)===requestedPf)||null;}
function inferCategory(name=''){const n=String(name).toLowerCase();if(/party\s*(?:till|til)\s*hell|hayati|anew|tee|shirt|hoodie|pullover|sweatshirt|sweater/.test(n))return'merch';if(/hat|cap|jacket|short|pant|skirt|bikini|swim/.test(n))return'fashion';return'other';}
function variantPrice(v){const p=Number(v?.price??v?.retail_price);return Number.isFinite(p)?p:null;}
function productPrice(p){const prices=(p?.variants||[]).map(variantPrice).filter(Number.isFinite);return prices.length?Math.min(...prices):null;}
function productImage(p){const candidates=[p?.thumbnail_url,p?.image_url];for(const v of p?.variants||[]){candidates.push(v?.image,v?.catalog_image,v?.product?.image);for(const f of v?.files||[])candidates.push(f?.preview_url,f?.thumbnail_url);}return candidates.map(safe).find(Boolean)||'';}
function productHref(p){const id=Number(p?.id||requestedPf);return id?`product.html?v=20260910k&id=printful-${id}&pf=${id}`:'index.html#catalog';}
function saveRelatedSnapshot(p){try{sessionStorage.setItem(SNAPSHOT_KEY,JSON.stringify({ts:Date.now(),id:`printful-${p.id}`,pfid:Number(p.id),source:p}));}catch{}}
function currentMeta(){const p=currentSource();if(!p)return null;const id=requestedId||`printful-${requestedPf}`;return{id,name:p.name||app.querySelector('.product-title')?.textContent?.trim()||'STARGIRLS ITEM',price:app.querySelector('.price')?.textContent?.trim()||(Number.isFinite(productPrice(p))?money(productPrice(p)):''),image:productImage(p),href:productHref(p),pfid:Number(p.id||requestedPf)};}
function normalizeWishes(){const raw=read(WISH_KEY,[]);if(!Array.isArray(raw))return[];return raw.map(x=>typeof x==='string'?{id:x,name:'STARGIRLS ITEM',href:'#'}:x).filter(x=>x&&x.id);}
function isSaved(id){return normalizeWishes().some(x=>String(x.id)===String(id));}
function toggleSaved(button){const meta=currentMeta();if(!meta)return;let list=normalizeWishes();const found=list.some(x=>String(x.id)===String(meta.id));if(found)list=list.filter(x=>String(x.id)!==String(meta.id));else list=[{...meta,savedAt:Date.now()},...list.filter(x=>String(x.id)!==String(meta.id))].slice(0,24);write(WISH_KEY,list);syncSaveButton(button);celebrate(button,found?'☆':'♥');showToast(found?'REMOVED FROM SAVED':'SAVED FOR LATER ♥');}
function syncSaveButton(button=app.querySelector('.product-save')){const meta=currentMeta();if(!button||!meta)return;const saved=isSaved(meta.id);button.classList.toggle('saved',saved);button.setAttribute('aria-pressed',saved?'true':'false');button.innerHTML=saved?'♥ SAVED':'♡ SAVE FOR LATER';}
function trackRecent(){const meta=currentMeta();if(!meta)return;const old=read(RECENT_KEY,[]);const list=Array.isArray(old)?old:[];write(RECENT_KEY,[{...meta,viewedAt:Date.now()},...list.filter(x=>String(x?.id)!==String(meta.id))].slice(0,6));}
function celebrate(target,symbol='★'){if(!target||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=target.getBoundingClientRect(),burst=document.createElement('span');burst.className='sg-burst';burst.style.left=`${r.left+r.width/2}px`;burst.style.top=`${r.top+r.height/2}px`;for(let i=0;i<8;i++){const s=document.createElement('i');s.textContent=symbol;s.style.setProperty('--a',`${i*45}deg`);s.style.setProperty('--d',`${26+(i%3)*10}px`);burst.appendChild(s);}document.body.appendChild(burst);setTimeout(()=>burst.remove(),720);try{navigator.vibrate?.(20)}catch{}}
function showToast(text){let t=document.querySelector('.conversion-toast');if(!t){document.body.insertAdjacentHTML('beforeend','<div class="conversion-toast" role="status" aria-live="polite"></div>');t=document.querySelector('.conversion-toast');}t.textContent=text;t.classList.add('show');clearTimeout(window.__sgConversionToast);window.__sgConversionToast=setTimeout(()=>t.classList.remove('show'),1500);}
function injectTrust(){const info=app.querySelector('.product-info'),price=info?.querySelector('.price');if(!info||!price||info.querySelector('.conversion-trust'))return;price.insertAdjacentHTML('afterend','<div class="conversion-availability">AVAILABLE NOW</div><div class="conversion-trust" aria-label="Purchase reassurance"><div class="conversion-trust-item"><span class="conversion-trust-icon">★</span><div><strong>OFFICIAL STARGIRLS</strong><span>Direct from the STARGIRLS store</span></div></div><div class="conversion-trust-item"><span class="conversion-trust-icon">✓</span><div><strong>SECURE CHECKOUT</strong><span>Protected payment through Stripe</span></div></div><div class="conversion-trust-item"><span class="conversion-trust-icon">↗</span><div><strong>SHIPPING SHOWN FIRST</strong><span>See shipping before you pay</span></div></div></div>');const note=info.querySelector('.product-note');if(note&&!info.querySelector('.purchase-nudge'))note.insertAdjacentHTML('beforebegin','<div class="purchase-nudge"><strong>MADE TO ORDER.</strong> Choose the exact color and size you want, then we send the order to fulfillment.</div><div class="conversion-reassurance"><span>No account required</span><span>Order confirmation by email</span><span>Secure payment</span></div>');}
function injectSave(){const info=app.querySelector('.product-info');if(!info||info.querySelector('.product-save'))return;const title=info.querySelector('.product-title');if(!title)return;title.insertAdjacentHTML('afterend','<button type="button" class="product-save" aria-pressed="false">♡ SAVE FOR LATER</button>');const b=info.querySelector('.product-save');b.addEventListener('click',()=>toggleSaved(b));syncSaveButton(b);}
function enhanceButtons(){const add=app.querySelector('#add'),buy=app.querySelector('#buyNow');if(add){add.classList.add('primary-conversion');if(!add.disabled)add.textContent='ADD TO CART';if(add.dataset.delight!=='1'){add.dataset.delight='1';add.addEventListener('click',()=>{if(add.disabled)return;celebrate(add,'★');showToast('ADDED TO YOUR BAG ★');setTimeout(updateCartMomentum,40);});}}if(buy){buy.classList.add('secondary-conversion');if(!buy.disabled)buy.textContent='BUY NOW — SECURE CHECKOUT';}app.querySelectorAll('.choice').forEach(c=>{if(c.dataset.delight==='1')return;c.dataset.delight='1';c.addEventListener('click',()=>{c.classList.add('choice-pop');setTimeout(()=>c.classList.remove('choice-pop'),220);});});}
function injectCartTrust(){const foot=document.querySelector('.cart-foot');if(!foot||foot.querySelector('.cart-conversion-trust'))return;foot.insertAdjacentHTML('beforeend','<div class="cart-conversion-trust"><span>Secure Stripe checkout</span><span>Shipping shown before payment</span><span>No account required</span></div>');}
function updateCartMomentum(){const foot=document.querySelector('.cart-foot');if(!foot)return;let box=foot.querySelector('.product-cart-momentum');if(!box){foot.insertAdjacentHTML('afterbegin','<div class="product-cart-momentum"><strong data-product-bag-count>YOUR BAG IS WAITING.</strong><span data-product-bag-note>Choose a piece and checkout stays simple.</span><a href="index.html#catalog">CONTINUE SHOPPING</a></div>');box=foot.querySelector('.product-cart-momentum');}const c=read('stargirls-cart',[]),count=Array.isArray(c)?c.reduce((n,x)=>n+Math.max(0,Number(x?.quantity||0)),0):0;box.querySelector('[data-product-bag-count]').textContent=count?`${count} ${count===1?'PIECE':'PIECES'} IN YOUR BAG ★`:'YOUR BAG IS WAITING.';box.querySelector('[data-product-bag-note]').textContent=count?'Review your selection, then checkout securely.':'Choose a piece and checkout stays simple.';}
function relatedProducts(){const cat=readCatalog();if(!cat)return[];const current=currentSource(),currentCat=inferCategory(current?.name||'');const all=(cat.products||[]).filter(p=>Number(p.id)!==requestedPf&&Array.isArray(p.variants)&&p.variants.length&&productImage(p));const same=all.filter(p=>inferCategory(p.name)===currentCat);return uniq([...same,...all]).slice(0,3);}
function injectRelated(){if(app.querySelector('.related-products'))return;const items=relatedProducts();if(!items.length)return;const cards=items.map(p=>{const img=productImage(p),price=productPrice(p),id=Number(p.id);return `<a class="related-card" href="product.html?v=20260910k&id=printful-${id}&pf=${id}" data-related-pf="${id}"><div class="related-image"><img src="${esc(img)}" alt="${esc(p.name||'STARGIRLS product')}" loading="lazy" decoding="async"></div><div class="related-copy"><strong>${esc(p.name||'STARGIRLS PIECE')}</strong><span>${Number.isFinite(price)?money(price):'VIEW ITEM'} →</span></div></a>`;}).join('');app.insertAdjacentHTML('beforeend',`<section class="related-products"><div class="related-head"><div><p class="related-eyebrow">KEEP GOING</p><h2>COMPLETE THE DROP</h2></div><p>More from the same STARGIRLS world. Tap an item to see its options.</p></div><div class="related-grid">${cards}</div></section>`);const cat=readCatalog();app.querySelectorAll('[data-related-pf]').forEach(a=>a.addEventListener('click',()=>{const p=(cat?.products||[]).find(x=>Number(x.id)===Number(a.dataset.relatedPf));if(p)saveRelatedSnapshot(p);}));}
function scrollToMissingOption(){const options=[...app.querySelectorAll('.option')];const missing=options.find(opt=>opt.querySelector('.choices')&&!opt.querySelector('.choice.active'))||app.querySelector('.buy-row');missing?.scrollIntoView({behavior:'smooth',block:'center'});}
function injectSticky(){let bar=document.querySelector('.mobile-sticky-buy');if(!bar){document.body.insertAdjacentHTML('beforeend','<div class="mobile-sticky-buy"><div class="sticky-product-copy"><strong data-sticky-name>STARGIRLS</strong><span data-sticky-price></span></div><button class="sticky-buy-button" type="button" data-sticky-buy>SELECT OPTIONS</button></div>');bar=document.querySelector('.mobile-sticky-buy');bar.querySelector('[data-sticky-buy]').onclick=()=>{const add=app.querySelector('#add');if(add&&!add.disabled)add.click();else scrollToMissingOption();};}const title=app.querySelector('.product-title')?.textContent?.trim()||'STARGIRLS',price=app.querySelector('.price')?.textContent?.trim()||'',add=app.querySelector('#add');bar.querySelector('[data-sticky-name]').textContent=title;bar.querySelector('[data-sticky-price]').textContent=price;const btn=bar.querySelector('[data-sticky-buy]');btn.disabled=false;btn.textContent=add&&!add.disabled?'ADD TO CART':'SELECT OPTIONS';}
function apply(){if(!app.querySelector('.product-info'))return;injectTrust();injectSave();enhanceButtons();injectCartTrust();injectRelated();injectSticky();updateCartMomentum();trackRecent();}
let scheduled=false;function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;apply();});}
new MutationObserver(schedule).observe(app,{childList:true,subtree:true});
const cartItems=document.getElementById('cartItems');if(cartItems)new MutationObserver(updateCartMomentum).observe(cartItems,{childList:true});
schedule();
})();